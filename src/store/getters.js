const getters = {
  items(state) {
    return state.items;
  },
  showPhotos(state) {
    return state.showPhotos;
  },
  activePhotos(state) {
    return state.activePhotos;
  },
};

export default getters;
